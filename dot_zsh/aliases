alias ls="ls --color"

alias update-initramfs="sudo mkinitcpio -p linux"

#alias yay='/usr/bin/yay --pacman powerpill'

alias updatemirrors='sudo reflector --country United\ States -l 200 --sort rate --save /etc/pacman.d/mirrorlist-arch'

alias ssh='TERM=xterm ssh'

alias pakt='mosh pakt -- byobu'

[ -r "$ZDOTDIR/aliases.private" ] && source $ZDOTDIR/aliases.private